<?xml version="1.0" encoding = "UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.soft.shopping.mapper.BoardMapper">
    <select id="list" resultType="BoardListDTO">
        SELECT
            BOARD_IDX
            , USER_ID
            , TITLE
            , CONTENT
            , CREATE_AT
        FROM
            CYCI_BOARD
    </select>

    <insert id="register" parameterType="BoardRegisterDTO">
        INSERT INTO CYCI_BOARD (USER_ID, TITLE, CONTENT, CREATE_AT) VALUES
        (#{userId}, #{title}, #{content}, SYSDATE())
    </insert>

    <select id="login" parameterType="String" resultType="MemberLoginIdxDTO">
        SELECT
            USER_IDX, USER_PW
        FROM
            CYCI_USER
        WHERE
            USER_ID = #{param}
    </select>
    <insert id="memberRegister" parameterType="MemberRegisterDTO">
        INSERT INTO CYCI_USER (USER_ID, USER_PW, USER_EMAIL) VALUES
        (#{userId}, #{userPw}, #{userEmail})
    </insert>
</mapper>
